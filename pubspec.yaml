name: first_project
description: A simple Dart project initialized by Copilot.
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Add project dependencies here

dev_dependencies:
  # Add development dependencies here
